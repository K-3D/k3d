INCLUDE_DIRECTORIES(${K3D_GTKMM_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${K3D_GTKGLEXT_INCLUDE_DIRS})

K3D_BUILD_MODULE(nodeGraph)
K3D_CREATE_MODULE_PROXY(nodeGraph)

TARGET_LINK_LIBRARIES(nodeGraph k3dsdk-ngui)

IF(K3D_BUILD_NLS)
	INCLUDE_DIRECTORIES(${K3D_INTL_INCLUDE_DIRS})
	TARGET_LINK_LIBRARIES(nodeGraph ${K3D_INTL_LIBS})
ENDIF(K3D_BUILD_NLS)
