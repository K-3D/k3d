INCLUDE_DIRECTORIES(${k3d_SOURCE_DIR}/k3dsdk_ngui)
INCLUDE_DIRECTORIES(${K3D_GTKMM_INCLUDE_DIRS})
INCLUDE_DIRECTORIES(${K3D_CAIROMM_INCLUDE_DIRS})

K3D_BUILD_MODULE(k3d-ngui-pipeline)
K3D_CREATE_MODULE_PROXY(k3d-ngui-pipeline)

TARGET_LINK_LIBRARIES(k3d-ngui-pipeline k3dsdk-ngui)
#TARGET_LINK_LIBRARIES(k3d-ngui-pipeline ${K3D_GTKMM_LIBS})
TARGET_LINK_LIBRARIES(k3d-ngui-pipeline ${K3D_CAIROMM_LIBS})

IF(K3D_BUILD_NLS)
	INCLUDE_DIRECTORIES(${K3D_INTL_INCLUDE_DIRS})
	TARGET_LINK_LIBRARIES(k3d-ngui-pipeline ${K3D_INTL_LIBS})
ENDIF(K3D_BUILD_NLS)

