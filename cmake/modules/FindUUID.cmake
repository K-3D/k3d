SET(K3D_UUID_FOUND 0)

IF(UNIX)
	INCLUDE(FindPkgConfig)
	PKG_CHECK_MODULES(UUID uuid)

	IF(UUID_FOUND)
		SET(K3D_UUID_INCLUDE_DIRS
			${UUID_INCLUDE_DIRS}
			)

		SET(K3D_UUID_LIB_DIRS
			${UUID_LIB_DIRS}
			)

		SET(K3D_UUID_LIBS
			${UUID_LIBS}
			)

		SET(K3D_UUID_FOUND 1)

	ENDIF(UUID_FOUND)

ENDIF(UNIX)

