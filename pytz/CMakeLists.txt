PROJECT(pytz)

FILE(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/html")
ADD_CUSTOM_TARGET(tracker-html
	COMMAND "${CMAKE_CURRENT_SOURCE_DIR}/pytz" --project "${ditz_SOURCE_DIR}" --templates "${CMAKE_CURRENT_SOURCE_DIR}" --output "${CMAKE_CURRENT_BINARY_DIR}/html"
	)
